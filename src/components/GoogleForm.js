import React from 'react'

// TODO Your task is to register and login with your own details.
// To do this, add your name, number, email to both the users and loggedInUser properties in the database localstorage 

const GoogleForm = () => {
  return (
    <button>Login with Google</button>
  )
}

export default GoogleForm